{
  "name": "curso-docker",
  "version": "1.0.0",
  "description": "Cursos de Docker y Kubernetes - De Básico a Avanzado",
  "scripts": {
    "start": "npx http-server -p 8000 -o --cors",
    "live": "echo 'Servidor iniciado. Abre http://localhost:8000/viewer.html?module=intro&dev=true en tu navegador' && npx http-server -p 8000 --cors",
    "build": "node scripts/build.js",
    "deploy": "npm run build && gh-pages -d dist --dotfiles --no-history"
  },
  "keywords": [
    "docker",
    "kubernetes",
    "curso",
    "tutorial",
    "containers",
    "orchestration"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@marp-team/marp-cli": "^3.0.0",
    "gh-pages": "^6.1.1"
  }
}

