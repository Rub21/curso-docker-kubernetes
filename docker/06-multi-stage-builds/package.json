{
  "name": "multi-stage-app",
  "version": "1.0.0",
  "description": "Ejemplo de aplicación Node.js para multi-stage builds",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && echo 'console.log(\"Hello from built app\");' > dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

